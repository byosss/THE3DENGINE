import engine.types;

import std;

int main() {
    
    engine::u32 a = 5;
    engine::i32 b = -5;
    engine::usize c = 10;

    std::println("test: a = {}, b = {}, c = {}", a, b, c);

    return 0;
}